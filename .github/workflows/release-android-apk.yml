name: Publish Latest Android APK

on:
  push:
    branches:
      - ride-pulse
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-android-apk
  cancel-in-progress: true

jobs:
  publish-latest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate prebuilt APK
        run: |
          test -s releases/RidePulse-latest.apk
          unzip -t releases/RidePulse-latest.apk >/dev/null

      - name: Prepare release files
        run: |
          mkdir -p dist
          cp releases/RidePulse-latest.apk dist/RidePulse-latest.apk
          APK_SHA256="$(sha256sum dist/RidePulse-latest.apk | cut -d ' ' -f 1)"
          {
            echo "Latest locally built Android APK"
            echo ""
            echo "- Branch: ${GITHUB_REF_NAME}"
            echo "- Commit: ${GITHUB_SHA}"
            echo "- APK SHA-256: ${APK_SHA256}"
            echo "- Published: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          } > dist/release-notes.md

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v7
        with:
          name: RidePulse-latest-apk
          path: dist/RidePulse-latest.apk
          if-no-files-found: error

      - name: Publish latest release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f latest "${GITHUB_SHA}"
          git push origin latest --force

          if gh release view latest >/dev/null 2>&1; then
            gh release edit latest --title "RidePulse Latest APK" --notes-file dist/release-notes.md --latest
            gh release upload latest dist/RidePulse-latest.apk --clobber
            gh release delete-asset latest Duke-Ride-latest.apk --yes || true
          else
            gh release create latest dist/RidePulse-latest.apk --title "RidePulse Latest APK" --notes-file dist/release-notes.md --latest
          fi
